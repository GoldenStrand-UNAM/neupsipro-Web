class UserSummaryDTO {
    constructor(userSummary) {
        this.id = userSummary.id;
        this.folio    = userSummary.folio ?? "Sin folio";
        this.fullName = userSummary.fullName;
    }
}
module.exports = UserSummaryDTO;