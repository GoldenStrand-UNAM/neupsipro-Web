<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Usuario - NeuPsi-Pro</title>
    <link rel="stylesheet" href="/css/output.css">
</head>

<body class="bg-[#002B7A] flex h-screen lg:flex-row transition-transform duration-300">

    <div id="overlay" class="fixed inset-0 bg-black/40 hidden md:hidden"></div>

    <!-- Sidebar -->
     <%- include('../partials/sidebar.ejs') %>

    <div class="flex-1 flex flex-col overflow-hidden">

        <!-- Header -->
        <%- include('../partials/header.ejs') %>

        <!-- Main -->
        <main class="flex-1 p-6 pb-0 md:pl-0 overflow-hidden">
            <div class="w-full h-full bg-white rounded-3xl p-6 overflow-y-auto space-y-6 mt-6">

                <!--Main Header-->
                <div class="sticky top-0 z-30 bg-white pb-4">
                    <h1 class="text-xl font-bold">
                        <svg class = "black-icon shrink-0 size-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="m11.25 9-3 3m0 0 3 3m-3-3h7.5M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                        </svg>

                        <span id="id_user"></span> > Entrevista Inicial

                    </h1>
                    <hr class="border-2 border-[#002B7A]">
                </div>
                
                <div class="w-full pt-0 p-3">                    

                    <!--Sections-->
                    <div id="sections-stepper" class="sticky top-10 z-50 bg-white pb-4">
                        <%- include('../partials/sections.ejs') %>
                    </div>

                    <div class="grid md:grid-cols-2">
                        <div>
                            <h1 id="step-title" class="font-bold section_title"></h1>
                        </div>

                        <!--Navegation buttons-->
                        <div class="pt-4 pr-6 p-1 flex justify-end gap-2">
                            <!--Previus-->
                            <button class="prevBtn btn-mini bg-[#3350A9]">
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="m11.25 9-3 3m0 0 3 3m-3-3h7.5M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                                </svg>

                                <span>Anterior</span>
                            </button>

                            <!--Next-->
                            <button class="nextBtn btn-mini bg-[#3350A9]">
                                <svg class="white-icon-mini" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="m12.75 15 3-3m0 0-3-3m3 3h-7.5M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                                </svg>

                                <span>Siguiente</span>
                            </button>
                        </div>
                    </div>

                    <!-- Section Title -->
                    <h2 id="subStep-title" class="section_title pl-60"></h2>

                    <!-- Content -->
                    <div class="grid md:grid-cols-5">

                        <!-- Sidebar -->
                        <div id="sidebar-timeline" class="z-10">
                            <%- include('../partials/sectionsSidebar.ejs') %>
                        </div>

                        <div id="dynamic-content" class="col-span-4">

                            <!-- FINANCIAL 2.1 -->
                            <div id="financial-section-1" class="financial-content hidden">
                                <div class="grid md:grid-cols-2 gap-4">

                                    <div>
                                        <%- include('./financial/subStep1/incomes.ejs') %>
                                    </div>

                                    <div>
                                        <%- include('./financial/subStep1/expenses.ejs') %>
                                    </div>

                                </div>
                            </div>

                            <!-- FINANCIAL 2.2 -->
                            <div id="financial-section-2"
                                class="financial-content hidden">

                                <div>
                                    <%- include('./financial/subStep2/esc.ejs') %>
                                </div>

                            </div>

                            <!-- FINANCIAL 2.3 -->
                            <div id="financial-section-3"
                                class="financial-content hidden">

                                <div>
                                    <%- include('./financial/subStep3/amai.ejs') %>
                                </div>
                            </div>

                            <!-- FINANCIAL 2.4 -->
                            <div id="financial-section-4"
                                class="financial-content hidden">

                                <div>
                                    <%- include('./financial/subStep4/results.ejs') %>
                                </div>
                            </div>

                        </div>

                    </div>

                    <!--Navegation buttons-->
                    <div class="pt-4 pr-6 p-1 flex justify-end gap-2">
                        <!--Previus-->
                        <button class="prevBtn btn-mini bg-[#3350A9]">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                                <path stroke-linecap="round" stroke-linejoin="round" d="m11.25 9-3 3m0 0 3 3m-3-3h7.5M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                            </svg>

                            <span>Anterior</span>
                        </button>

                        <!--Next-->
                        <button class="nextBtn btn-mini bg-[#3350A9]">
                            <svg class="white-icon-mini" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                                <path stroke-linecap="round" stroke-linejoin="round" d="m12.75 15 3-3m0 0-3-3m3 3h-7.5M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                            </svg>

                            <span>Siguiente</span>
                        </button>
                    </div>
                </div>
                
            </div>
        </main>

    </div>

    <!-- Script -->
    <script src="/js/financialMapper.js"></script>
    <script src="/js/sidebar.js"></script>
    <script src="/js/sectionsStepper.js"></script>
    <script src="/js/sidebarTimeline.js"></script>
    
    <script>
        // Functions that set titles ---------------------------------------

        // Set by step
        function setStepTitle(data) {
            const steptitle = document.getElementById('step-title');

            if (data.current_step === 1) {

                steptitle.textContent =
                    '1. Ficha de Identificación';

            } else if (data.current_step === 2) {

                steptitle.textContent =
                    '2. Situación Financiera';

            } else if (data.current_step === 3) {

                steptitle.textContent =
                    '3. Historia Clínica';

            } else {

                steptitle.textContent =
                    'Sección no encontrada';
            }
        }

        // Set by Substep
        function setSubStepTitle(data) {
            const subStepTitle = document.getElementById('subStep-title');

            if (data.current_step === 2) {

                if (data.current_section === 1) {
                    subStepTitle.textContent = '2.1 Ingresos y Egresos';

                } else if (data.current_section === 2) {
                    subStepTitle.textContent = '2.2 ESC Gubernamental';

                } else if (data.current_section === 3) {
                    subStepTitle.textContent = '2.3 Cuestionario AMAI';

                } else if (data.current_section === 4) {
                    subStepTitle.textContent = '2.4 Resumen de Resultados';
                }
            }
        }

        // Functions that set content ---------------------------------------

        // Financial Substep1 Content
        function setFinancialContent(data) {

            // Hidde all
            document.querySelectorAll('.financial-content')
                .forEach(section => {
                    section.classList.add('hidden');
                });

            // Chance the correct substep
            const activeSection =
                document.getElementById(
                    `financial-section-${data.current_section}`
                );

            if (activeSection) {
                activeSection.classList.remove('hidden');
            }
        }
        
        // Set Content
        function setContent(data) {
            if(data.current_step === 1){
                // missing
            } else if (data.current_step === 2) {
                setFinancialContent(data);
            } 
        }

        const id_user = "<%= id_user %>";

        async function loadFinancial() {
            try {
                const res = await fetch(`/users/${id_user}/inicialInterview/api/financial/${id_user}`);
                const data = await res.json();

                if (!res.ok) {
                    console.error(data.error);
                    return;
                }

                // Set titles
                setStepTitle(data);
                setSubStepTitle(data);

                // Render correct section
                setContent(data);

                // Save global data
                window.financialData = data;
                console.log(data);

                // Wait for DOM render and fill inputs
                setTimeout(() => {
                    window.renderFinancialData?.();
                }, 0);
                
                document.getElementById("id_user").textContent = data.id_user || "";

                window.sidebarState = {
                    currentStep: data.current_section,
                    completedSteps: data.data.completedSubSteps,
                };

                window.sectionsState = {
                    currentStep: data.current_step,
                    completedSteps: data.data.completedSteps,
                    subStep: data.current_section
                };

                if (window.renderSidebar) {
                    window.renderSidebar();
                }

                if (window.renderSections) {
                    window.renderSections();
                }

            } catch (err) {
                console.error(err);
            }
        }

        loadFinancial();

        // Listener for change substep
        document.querySelectorAll('.timeline-step').forEach(step => {
            step.addEventListener('click', () => {

                const section = Number(step.dataset.step);

                // Update current section
                window.financialData.current_section = section;

                // update titles
                setSubStepTitle(window.financialData);

                // update content
                setFinancialContent(window.financialData);

                // update sidebar
                window.sidebarState.currentStep = section;

                if (window.renderSidebar) {
                    window.renderSidebar();
                }
            });
        });

        // NEXT buttons
        document.querySelectorAll('.nextBtn').forEach(button => {

            button.addEventListener('click', () => {

                let current = window.financialData.current_section;

                // max section = 4
                if (current < 4) {

                    current++;

                    window.financialData.current_section = current;

                    setSubStepTitle(window.financialData);
                    setFinancialContent(window.financialData);

                    window.sidebarState.currentStep = current;

                    if (window.renderSidebar) {
                        window.renderSidebar();
                    }
                }
            });
        });


        // PREVIOUS buttons
        document.querySelectorAll('.prevBtn').forEach(button => {

            button.addEventListener('click', () => {

                let current = window.financialData.current_section;

                // min section = 1
                if (current > 1) {

                    current--;

                    window.financialData.current_section = current;

                    setSubStepTitle(window.financialData);
                    setFinancialContent(window.financialData);

                    window.sidebarState.currentStep = current;

                    if (window.renderSidebar) {
                        window.renderSidebar();
                    }
                }
            });
        });
    </script>

</body>
</html>