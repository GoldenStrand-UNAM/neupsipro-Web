<div class="space-y-6">
  <h2 class="section_title">3.3 Sensorial</h2>
  <div class="mt-4 space-y-4">

    <!-- sensation loss -->
    <div>
      <label class="input_label">Pérdida de la sensación (indicar parte del cuerpo)</label>
      <div class="flex gap-2 mt-1">
        <select id="sensationLoss" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="sensationLossNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

    <!-- sight dificulty -->
    <div>
      <label class="input_label">Dificultad para ver</label>
      <div class="flex gap-2 mt-1">
        <select id="visionDifficulty" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="visionDifficultyNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

    <!-- uses glasses -->
    <div>
      <label class="input_label">Usa lentes</label>
      <div class="flex gap-2 mt-1">
        <select id="usesGlasses" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="usesGlassesNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

    <!-- blurry -->
    <div>
      <label class="input_label">Vista borrosa</label>
      <div class="flex gap-2 mt-1">
        <select id="blurredVision" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="blurredVisionNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

    <!-- hearing difficulty -->
    <div>
      <label class="input_label">Dificultad para escuchar</label>
      <div class="flex gap-2 mt-1">
        <select id="hearingDifficulty" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="hearingDifficultyNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

    <!-- ringing -->
    <div>
      <label class="input_label">Zumbido en los oídos</label>
      <div class="flex gap-2 mt-1">
        <select id="earRinging" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="earRingingNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

    <!-- ear pain -->
    <div>
      <label class="input_label">Dolor en los oídos</label>
      <div class="flex gap-2 mt-1">
        <select id="earPain" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="earPainNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

    <!-- phantom limb -->
    <div>
      <label class="input_label">Presencia de miembro fantasma (describir tipo y frecuencia)</label>
      <div class="flex gap-2 mt-1">
        <select id="phantomLimb" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="phantomLimbNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

    <!-- painfull phantom limb -->
    <div>
      <label class="input_label">Presencia de miembro fantasma doloroso (describir tipo y frecuencia)</label>
      <div class="flex gap-2 mt-1">
        <select id="painfulPhantomLimb" class="input w-40 flex-shrink-0 bg-white border border-gray-300 rounded-md text-sm text-gray-500" disabled>
          <option value="" disabled selected>Selecciona una opción</option>
          <option value="si">Sí</option>
          <option value="no">No</option>
        </select>
        <input type="text" id="painfulPhantomLimbNotes" placeholder="Notas"
          class="input flex-1 bg-white border border-gray-300 rounded-md text-sm text-gray-500 placeholder-gray-400" disabled/>
      </div>
    </div>

  </div>
</div>